---
import LegalLayout from '../../layouts/LegalLayout.astro';
import { renderLocalizedMarkdownFile } from '../../lib/markdown';
import { locales, type Locale } from '../../types/locale';

export function getStaticPaths() {
	return locales.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;
const html = await renderLocalizedMarkdownFile('content/terms.md', locale);
---

<LegalLayout title="Terms of Service | Voquill" description="Voquill terms of service" locale={locale}>
	<Fragment set:html={html} />
</LegalLayout>
